# RoundTable execution entrypoint

RoundTable indexes work across the owner’s projects; product source stays in its own repository.

## Owner-delegated scope

The owner has requested hands-on implementation and verification across all their projects, not only TradeScout. Use the currently authorized PC terminal, Chrome-debug session, repository connection, or project runtime when available. Do not ask for the same project-access permission again. Access must still be verified by an actual tool response; an inventory entry or a reported reconnect is not proof of a usable session.

Use `docs/ALL_REPOS_OPERATION.md` and `CONNECTOR_POLICY.md`. The shared registry loader covers the entire supplied owner-scoped connector inventory, including private repositories, without copying private metadata into this public repository. Existing local paths are historical hints until the actual root, remote and commit are checked. Unknown paths stay unknown.

## SI / Infinity continuation

Resume each project’s latest authoritative checkpoint and current branch before reading more source. Use SI’s existing execution rules and Infinity’s existing continuity state where installed; never claim installation or cross-session operation from repository files alone. Preserve the exact next unproven action and its evidence. Do not replace these systems with another scheduler.

Keep independent implementation lanes moving without repeated repository-wide audits. Share one verified connectivity observation during a task instead of retrying the same unavailable device separately for every project. Do not overwrite concurrent or uncommitted work, invent local workspaces, or infer that the PC is powered off from a disconnected connector.

## Execution and release boundaries

An all-repository preflight is a read-only checkout check, not product testing, browser proof, model dispatch, installation, or deployment. Source checks, unit tests, native database tests, real browser journeys and production checks must be reported separately and tied to their actual revision. Empty, failed or unexecuted checks cannot become success.

Use isolated worktrees and disposable fixtures for tests. Preserve each product’s contact, verification, payment and release controls. Do not place live trades, make real purchases, send customer messages, publish private information, bypass a rejected operation, or change access controls under a general implementation instruction. Ordinary source work and approved release paths remain available within their established rules.

Before yielding, save the current branch/commit, completed work, unverified work, commands/results, external side effects, next exact action, and work that must not be repeated. Do not claim asynchronous execution after the response ends.
